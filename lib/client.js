/**
 * IRC Main Client
 */

const Promise = require('bluebird')

class Client {
    constructor(options = {}) {
        this.options = options
    }
    start() {
        
    }
}

module.exports = Client
